export interface Schedule {
    day: number
    hour: number
    minute: number
    createdBy: string
    createdAt: string
    updatedBy: string
    updatedAt: string
}

export interface ScheduleForm {
    day: number
    hour: number
    minute: number
} 